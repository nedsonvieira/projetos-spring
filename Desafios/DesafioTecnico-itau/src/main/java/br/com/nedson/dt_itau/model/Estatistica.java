package br.com.nedson.dt_itau.model;

public record Estatistica(Long count,
                          Double sum,
                          Double avg,
                          Double min,
                          Double max) {
}
